name: Publish to NPM

on:
  push:
    branches:
      - main

jobs:
  publish:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2
        with:
          token: ${{ secrets.DEPENDABOT_SECRET }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 16

      - name: Install Dependencies
        run: npm install

      - name: Bump Version and Publish to NPM
        run: |
          git config --global user.name "Shantanu Mane"
          git config --global user.email "shantanu.mane.200@ooutlook.com"
          git add .
          git commit -m "Bump Version %s [skip ci]"
          npm version patch
          npm publish

      - name: Push Changes
        run: |
          git push origin main
          git push origin --tags
        env:
          GH_TOKEN: ${{ secrets.DEPENDABOT_SECRET }}
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
